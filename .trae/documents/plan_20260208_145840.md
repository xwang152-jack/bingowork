I will modify the `MessageList.tsx` component to add a functional toolbar to the message bubbles.

1.  **Update `src/components/cowork/MessageList.tsx`**:
    *   Import icons: `Copy`, `RotateCcw` (Retry), `Pencil` (Edit), `Trash2` (Delete), `Check` (Success feedback) from `lucide-react`.
    *   Modify `MessageItem` component:
        *   Add `isCopied` state for the copy feedback loop.
        *   Add `handleCopy` function using `navigator.clipboard.writeText`.
        *   Add a toolbar `div` below the message content.
        *   The toolbar will show:
            *   **Copy Button**: Available for all messages. Shows a Check icon briefly after clicking.
            *   **Retry Button**: Available for AI messages (visual only for now).
            *   **Edit Button**: Available for User messages (visual only for now).
            *   **Delete Button**: Available for all messages (visual only for now).
    *   Apply styling:
        *   Use `group` on the message container to show the toolbar on hover (`opacity-0 group-hover:opacity-100`) for a clean UI.
        *   Use consistent Tailwind colors (`text-stone-400`, `hover:text-stone-600`) to match the existing design.

This approach ensures the UI matches the request while providing immediate utility (Copy) and placeholders for future actions.